# Web framework
Flask==3.1.3
Werkzeug==3.1.3
itsdangerous==2.2.0

# CSRF protection (also pulls in a compatible WTForms automatically)
Flask-WTF==1.3.0

# Production WSGI server (used by Procfile on Render)
gunicorn==23.0.0

# Loads .env locally; harmless no-op on Render (env vars come from the dashboard)
python-dotenv==1.0.1

# Supabase client (PostgreSQL backend + optional Storage for deposit proofs)
supabase==2.15.3
httpx==0.28.1
